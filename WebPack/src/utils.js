// src/utils.js

/**
 * Retorna un string con el monto formateado a 2 decimales.
 * @param {number} monto 
 * @returns {string}
 */
export const formatearMonto = (monto) => {
    return monto.toFixed(2);
};